# core/maps.py
import streamlit as st

def render_map(T, ctx):
    st.markdown("#### 5) Mappa")
    st.info("Mappa avanzata verrà riattivata dopo il porting (Folium/OSM/Overpass).")

render = render_map
